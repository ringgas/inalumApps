import React from 'react';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Text } from 'react-native';

const Cart = () => (
  <SafeAreaView>
    <Text>Pages Cart</Text>
  </SafeAreaView>
);

export default Cart;
